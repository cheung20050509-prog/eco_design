# 第 4 章 金融量化分析工具准备－－  N u m p y 基础




Numpy 简介

Numpy 对象创建

Numpy 数组数据访问




Numpy 数组操作

Numpy 模块的主要函数

Numpy 随机数处理


### 1 Numpy 简介

1.1 Numpy 主要优势

提供了强大的 N 维数组处理方法。 Numpy 的核心是多维数组对象

(ndarray) ，它可以通过矢量化、索引和广播概念实现高效存储和处理大量数
据，而且支持各种数组操作，例如切片、索引、转置、合并等等。

提供广播实现功能。 Numpy 通过广播功能可以让不同形状的数组进行运算，

而无须进行显示的复制数据，这极大地简化了代码的编写。

提供了丰富的数值计算以及统计函数。 Numpy 提供了全面的数学函数，包

括随机数生成器、线性代数、傅里叶变换等函数具备与其他科学计算模块的
高等级兼容性。 Numpy 与其他科学计算模块 ( 例如 Scipy 、 Pandas 等 ) 结合
使用，可以很方便地进行数据处理与科学计算等。

提供了高性能计算能力。由于 Numpy 的核心代码是优化良好的 C 代码，因

此 Numpy 既体现了 Python 的灵活性，又可以发挥 C 语言代码高速执行的优
势。

提供了多平台使用解决方案。 Numpy 支持广泛的硬件和计算平台，并且可

以很好地与分布式、 GPU 和稀疏数组模块配合使用  

### 1 Numpy 简介

1.2 Numpy 主要功能

以数组形式保存、处理数据： Numpy 的核心是 ndarray(N
dimensionalarray ，多维数组 ) 对象， Numpy 通过 ndarray 对象为各
领域数据提供了便捷的多维数组存储方式，这种数组结构可以进行各
类操作，包括切片、索引、转置、合并等操作。

数学运算： Numpy 提供了各种数学函数，包括三角函数、指数函数、

对数函数、幂函数、复数函数等等。 Numpy 也提供了各种线性代数
运算函数，可以方便的进行各类线性运算，例如向量和矩阵的乘法、
矩阵的逆和行列式计算等。此外， Numpy 也提供了各类统计函数，
可以计算平均值、方差、标准差、最大值、最小值等各类统计量。

生成随机数： Numpy 提供了各种随机数生成函数，可以生成不同分

布的随机数，例如正态分布、均匀分布、泊松分布等等。


### 1 Numpy 简介

1.2 Numpy 底层结构
NumPy 的底层结构的核心是 ndarray(N-dimensional array ，多维数组 ) 对
象。 ndarray 对象是一个由同，以实现计算效率的大幅提升。 ndarray 类型元素组成
的多维数组，，其中每个元素占用固定大小的内存对象有以下几个重要属性 :

shape:ndarray 的形状，用一个整数元组表示，每个元素表示数组在每个维度上的

大小。例如，一个二维数组的 shape 属性为 (n,m) ，其中 n 表示数组在第 1 维度
上的大小， m 表示数组在第 2 维度上的大小。具体地说，对于一个一维数组，它
的 shape 只有一个元素，表示数组的长度 ; 对于一个二维数组，它的 shape 有两
个元素，分别表示数组在第 1 维度和第 2 维度上的大小, 对于一个三维数组，它
的 shape 有三个元素，分别表示数组在第 1 维度、第 2 维度和第 3 维度上的大小 ;
以此类推。

dtype:ndarray 中元素的数据类型， NumPy 支持的数据类型包括布尔型 (bool) 、整

数型 (int 、 int8 、 int16 、 int32 、 int64) 、无符号整数型
(uint 、 uint8 、 uint16 、 uint32 、 uint64) 、浮点型
(foat 、 float16 、 foat32 、 float64) 、复数型
(complex 、 complex64 、 complex128) 。

di d 对象的维数 ( 也称为秩 ) 即数组的轴数


### 2 Numpy 对象创建

2.1 创建 ndarry 对象


语法 :numpy.array(object, dtype=None,

copy=True, order='K', subok=False,
ndmin=0)

参数 :

用以指定 array 创建的数据来源。

  - dtype: 可选，数组的所需数据类型，

要复制数据，默认为 True 。

  - order: 字符串类型数据，可选。


### 2 Numpy 对象创建

2.2 创建特殊 ndarry 对象
1.empty 方法

值。
语法 :numpy.empty(shape,dtype=float, order=‘C”)
2.zeros 方法

- 功能 : 用于创建指定形状和数据类型的全零数
组。语法 :numpy.zeros(shape, dtype=float,
order='C’.
3.ones 方法

- 功能 : 用于创建指定形状和数据类型的全 1 数
组。语法 :numpy.ones(shape, dtype=float,
order='C”)


的索引号写入以列表形式保存，并逐个写入多维数组后的内
即可，每个元素的各维度索引号列表之间以逗号“," 分割。


### 3 Ndarry 数组数据访问


  - 如果需要访问指定行以后所有的数
据， ndarray 对象后的填入“行索引
号 :” 即可。


### 3 Ndarry 数组数据访问

3.2 切片
4. 布尔切片 Numpy

  - 可以通过布尔数组进行切片。布尔索引通
过布尔运算 ( 例如 : 比较运算符 ) 来获取符
合指定条件的元素的数组，
5. 花式切片

  - NumPy 的花式切片 fancyindexing) 是指使
用整数数组来进行索引，从而获取数组中
指定位置的元素。花式切片可以用于获取
数组中的任意一组元素，可以用于选择、
修改和重新排列数组中的元素。


### 4 Ndarry 数组操作

3. transpose 方法，

  - 功能 : 用于对 ndarray 数组进行转置操作。转置
操作会将 ndarrav 数组的行和列交换，也就是将
行变为列，列变为行。


### 4 Ndarry 数组操作

4.2 Ndarray 数组的合并

ndarray 数组按照指定轴进行合并

语


e).

参数 :

- a1,a2,…: 需要拼接的数组序列。

为一维数组进行拼接。

的数组，如果没有指定，就会新创建一个
数组来存储结果


### 4 Ndarry 数组操作

4.3 Ndarray 数组的分割

组

参数 :

- a: 需要被分割的数组。

后一个元素。

向进行分割


### 4 Ndarry 数组操作

1. 删除单一元素

  - 可以使用 delete 函数删除 ndarray 对象中
的数据， . 语法 :numpy.delete(arr,
obj,axis=None).
2. 删除指定行、列数据

  - 使用 delete 函数，可以指定要删除的轴，
例如删除第 1 行。。 3. 切片删除数据

  - 使用 delete 函数，也可以将 obi 参数设置
为切片或花式索引形式，实现删除指定多
组数据功能，例如删除第 1 列与第 3 列数
据。


### 4 Ndarry 数组操作

4.5 Ndarray 数组数据
添加添加整行或整列数据，可以使用 append
函数实现 :

语法 :numpy.append(arr,values,axis=None)

参数 :

- arr: 要操作的 ndarray 对象。

- values: 要添加的元素，可以是标量或
ndarray 对象

- axis: 指定要添加元素的轴。如果不指定，
则将数组展开为一维数组后添加元素


5.1 统计类函数

函数包括


### 5 Ndarry 模块的主要函数

5.2 线性代数类函数

的线性代数运算的函数包括 :

- numpy.dot(): 计算矩阵乘积。

- numpy.trace(): 计算矩阵的迹。

征向量。


### 5 Ndarry 模块的主要函数

5.3 排序与筛选函数

对应函数与方法，如

1.sort 函数


按指定轴排序后的数组副本。


参数 :

  - a: 要排序的数组。

着最后一个轴排序。

  - kind: 指定排序算法，可以取值

认为 'quicksort’ 。

  - order 如果数组包含字段 则指定要排序的字段。


### 6 Ndarry 随机数处理

6.1 Numpy 处理随机数问题的优势
Numpy 在处理随机数问题上具有非常大的优势，主要体现在以下方面 :

   - 一致性 :NumPv 提供了许多随机数生成器，这些生成器使用了相同的接口，
因此在使用时非常一致。这种一致性使得代码更易于编写、调试和维护。

   - 可重复性 :NumPy 的随机数生成器是伪随机的，但可以设置随机数种子，
使得随机数序列可以重复生成。这种可重复性在测试和调试时非常有用。

   - 灵活性 :NumPy 提供了多种随机数生成器，可以生成不同分布、不同范围
和不同形状的随机数。这种灵活性使得 NumPy 在各种数据分析和科学计算
任务中都非常有用。

   - 多维数组支持 :NumPy 可以生成多维随机数数组，这种多维数组在处理图
像、音频和视频数据等方面非常有用。

   - 算法丰富 :NumPy 提供了多种生成随机数的算法，包括梅森旋转算法
(MersenneTwister) 、互相关移位反馈寄存器算法 (xorshift) 、超几何分布等。
这种算法丰富性使得 NumPy 可以满足各种随机数生成的需求。


### 6 Ndarry 随机数处理


- numpy.random.gamma(): 生成一个指定形状和尺度参数的伽马分布的随机数，可以生成
指定形状的随机数数组。

- numpy.random.binomial(): 生成一个指定样本量和概率的二项分布的随机数，可以生成指
定形状的随机数数组。

- random.poisson(): 生成一个指定均值的泊松分布的随机数，可以生成指定形

- 状的随机数数组。


完成


6.4 随机模拟实验

2. 蒙特卡罗模拟实验

推断或回归分析等领域。


#### 思考题



1.Numpy 模块的 ndarray 对象的基本操作有哪些 ? 完成这些操作需要哪
些函数和方法 ?


2.Numpy 求解线性方程组的步骤什么 ? 具体如何实现 ?


3. 蒙特卡罗模型的原理是什么 ? 如何利用蒙特卡罗模型预测股票价格 ?


