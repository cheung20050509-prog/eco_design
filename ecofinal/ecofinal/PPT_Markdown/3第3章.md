# 第 3 章 金融量化分析工具准备－－ Pan das 基础


###### • Pandas 简介 • Series 对象 • DataFrame 对 象


###### • Pandas 金融量化分析应 用 • Pandas 数据可视化


##### 1 Pan d as 简
### 介

###### 1.1 Pan das 主要优势

   - 可处理格式多样的数据源文件。 Pandas 可从多种格式的数据文件中提取数据， 例如，
EXCE L 、 CSV 、 SQL 、 JSON 、 HTML 、 TXT 、 HDF5 、 XM L 、 SAS 等文件。

   - 函数功能丰富、调用简便。 Pandas 提供了丰富的数据处理方法与函数， 这些方法与函数涵
盖数 据清洗、数据类型转换、数据分组与聚合、数据合并与连接、数据可视化等功能， 这
使 Pandas 具备可以处理复杂数据分析工作的能力。

   - 功能 扩 展性高 。 Pandas 与 Numpy 、 Matplotlib 、 SK-learn 等专业应用模块高度集成 ，
Pandas 模 块方法可以与上述模块方法直接结合使用， 实现功能扩展、优势互补。

   - 数据处理运算速度高。由于 Pandas 模块底层基于高效科学计算模块 Numpy 实现， 自 身内部
采取 向 量化操作模式且支持并行计 算， 因此， Pandas 在处理金融量化分析数据具备强大
的高速运算 能力。


##### 1 Pan d as 简
### 介

###### 1.2 Pan d as 主要功能

   数据获取。 Pandas 提供 了从不同格式的文件中（例如， EXCE
L 、 CSV 、 SQL 、 JSON 、 HTML 、 TXT 、 HDF5 、 XM L 、 SAS 等格式文件）加载数据的方法。

   数据存储。 在获取外部数据后， Pandas 内部 以 Series 对象或 DataFrame 对象方式进行存储，
这两 种对象数据均具有索引属性， 可 进行快速查询、定位功能。

   - 数据清洗。 Pandas 通过 fillna 、 dropna 、 dup licated 、 drop_d uplicates 等方法进行数据清
洗。

   - 数据处理。

数据选择与过滤
数据统计与聚合
数据合并与拼接
数据结构变形与重构
时间序列处理


##### 1 Pan d as 简
### 介

###### 1.3 Pan d as 的底层结构

   - Pandas 的底层结构是由 Numpy 数组和自定义数据类型组成的， 其中， Series 对象和
DataFrame 对 象是 Pandas 自定的两类数据结构

   - Series 对象是—种带有标签的—维数组， 可以存储任意类型的数据。每个数据点都有—个与
之 对应的标签 —般称为索引。 Series 可以被视为— 个由索引和值组成的字典。

   - DataFrame 对象是由多个 Series 对象组成的表格型数据结构， 可以被视为— 个二维数组，
其中 每—列可以是不同的数据类型。 DataFrame 具有行索引和列索引， 可以进行行列操作，
支持多种 类型的数据操作。


#### 象


#### 象


#### 2 Series 对 象

###### 2.2 访问 Seri es 对象数据

   - 通常可以通过两种方式访问 Series 对象数据， — 种方法利用数据的位置索引访问， 另—种方
法利 用索引标签访问


#### 象


    在进行数


#### 2 Series 对 象

数据。


 - 参数：

认 为 5


#### 2 Series 对 象

2.4 Serie s 常用函数


(2) describe 函数


  - 参数：


或 ['number'
'd atet ime' ] 。


['o bject'
·c atego ry' ］。


#### 象












#### 象


 - 参数：

    
    
    

#### 2 Series 对 象


 - 参数：

    
    

#### 象


##### 3 DataFram e 对
#### 象

###### 3.1 DataFrame 对象数据存储结构

   - 数据区域 (Data) 存储数据本身， 通常是—个二维的 NumPy 数组 (numpy.ndarray) ， 每—列可以有
不同的数据类型。

   - 行索引 (Index) 用于唯—标识每—行， 通常是—个 pd.Index 对象或其子类，例如
pd.Rangelndex 。

   - 列索引 (Columns) 用于唯—标识每—列， 通常是—个 pd.Index 对象或其子类，例如
pd.Rangelndex 或 p d.M ultiIndex 。

   - 轴标签 (Axes):pd.DataFrame 对象同时具有行和列两个轴， 因此有两个轴标签，分别对应行 索引和
列索引。

   - 此外， p d .DataFrame 还有—些其他的属性，例如数据类型 (dtype) 、形状 (shape) 、内存信
息 (memory_usag e( ） ) 等。


#### 象


 

 

   
   签。

   - values:












几种









法


略索引， 如果设置为False, 即不忽True, 则 将排序后的结果重新生成索引。


#### 5 Pand as 数据可视化

1. 折线图


   图，






#### 5 Pand as 数据可视化

1. 折线图


   Pa nd as 通过调用 plot 函数可以制作折线
图，





2. 直方图

  hist 方 法完成。


#### 5 Pand as 数据可视化

1. 折线图


2. 直方图


- Pa nd as 可以通过两种方法绘制直方图， 一
种是直接通过 plot 方法绘制， 另一种是通
过 hist 方 法完成。


### 思考题：

1 如何将 CSV 文件数据存储入 SQLite 数据库文件？


2. Pandas 模块中 Series 对象与 DataFram e 对象之间的关系是什么？ 两者之间存在哪些相同点与不

同点？


3. DataFram e 对象列操作方法有 哪些？ 这些操作方法有何用途？


