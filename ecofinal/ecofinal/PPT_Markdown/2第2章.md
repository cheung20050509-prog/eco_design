# 第 2 章 金融量化分析工具准备－－基础
### 语 法




 Pyt hon 简
介

 Pyt hon 变

流程控制
量




 函数

类与对象

模块应用


##### 1 Python 简
#### 介

1.1 Pyt h on 数据处理优势

   - Python 具有简单易学和可读性强的语法结构，使得金融从业者能够快速上手。

   - Python 代码通常比其他语言的代码更简洁，更易于理解和维护。

   - Python 拥有丰富的第三方模块和工具，可以快速地进行数据分析、统计计算和机器学习等领域的
开发。例如， NumPy 、 Pandas 、 Matplotlib 等模块能够帮助量化分析人员进行数据处理和可
视化， Scikit-Learn 、 TensorFlow 等模块能够支持机器学习和深度学习的应用。

   - Python 是一种解释型语言，能够快速运行和调试代码，避免了编译的时间和烦琐的编译过程，这
对于迭代开发和快速原型开发非常有帮助。

   - Python 是开源的语言，可以免费使用，并且社区非常活跃，有很多开发者为其贡献代码和文档,
支持更新维护，因此在开发过程中可以得到更多的帮助和支持。


#### 量化交易的简单例子

《七大王牌指标必杀技》



10


10


10


##### 1 Python 简
#### 介

1.2 Pyt hon 基本语法


1 函数、变量、对象名称必须区分大小写


   - 在定义变量、调用函数等过程中， Python 要求进行区分大写与小写，例如函数名“ Buy()” 与
函数名“ buy()” 是两个不同的独立函数


2 标点符号必须使用英文字符输入


   - 在 Python 编程中，冒号、逗号、分号、括号、引号等各种标点符号必须用英文的，如果使
用中文输入这些标点符号则会报错


而｀＃”
后面




##### 1 Python 简
#### 介

1.2 Pyt hon 基本语法


5 允许在— 行中写多条语句


##### 量


##### 2 Python 变 量

2.1 数值型变量

   - 整数型变量 (int)): 用以表示整数类型数据。例如，持仓股票个数、股票交易次数等均可
用整数型 (int) 表示。

   - 浮点型变量 (float): 用以表示带有小数的数据。例如，股票收盘价价格、投资收益率等可
以使用浮点型变量 (float) 表示。


##### 2 Python 变 量

2.2 布尔类型变量

   布尔类型变量 (bool) 只有两个值， True 与 False ，分别表示“真”与“假”。由于布尔
对象 (bool) 是整数对象 (int) 的子类，布尔类型变量等价于整数型中的 1 与 0 ，即 True 与
1 等价， False 与 0 等价。


##### 2 Pyt hon 变 量

2.3 字符串类型变量


1 字符串类型变量


   - Python 一般用英文输入法下的双引号”、单引号’来包裹赋值的字符串类型变量的内容，有时为了是存储字符
串的内部格式，会使用三个双引号与单引号来包裹带有排版格式的字符串内容。


##### 2 Python 变 量

2.3 字符串类型变量


2 字符串类型变量常用函数与方法


##### 2 Python 变 量

2.3 字符串类型变量


2 字符串类型变量常用函数与方
法


(2) replace 函数

 - 功能： 将字符串变量中指定字符串进行替换，输出结果为替换后的字符串变量。


 - 参数：


##### 2 Python 变 量

2.3 字符串类型变量


2 字符串类型变量常用函数与方法


(3) strip 函数


 - 功能： 用于去除字符串开头和结尾的指定字符（默认为空格和换行符）。

 - 语法： str.strip([chars])


 - 参数：


##### 2 Python 变 量

**2.4** 字符串类型变量


2 字符串类型变量常用函数与方法

(4)join 函数

   - 功能 : 用于将序列中的元素连接成一个字符串。

   - 语法 :strjoin(iterable).

   - 参数 :iterable: 一个可选代对象，例如列表、元组、字符串等


##### 2 Python 变 量

**2.4** 字符串类型变量


2 字符串类型变量常用函数与方法


(5) count 函数


 功能 ： 用于计算某个子字符串 在当前字符串中出现的次数。


 语法： str.count(sub, start=O, end=len(string))

 参数 ：

 - str 要进行计数操作的字符串。

 - sup : 要查找的子字符串。

 - start: 开始查找的索引位置 ( 默认为 0)

 - end: 结束查找的索引位置 ( 默认为 len(string))


##### 2 Python 变 量

**2.5** 列表类型变量


1 列表变量


   在 Python 中，列表会将所有各类数据元素都放在一对中括号“ []" 里面，相邻数据元素之间
用逗号分隔。在金融量化分析中，常用列表变量保存各类序列数据，比如一段期间的日线序列
数据、策略收益率变动序列数据等。


##### 2.2 Python 变 量

2.2.5 列表类型变量


2 列表变量常用方法 （函数）


(1 ) app end 方法


 - 语法： I ist.app end(item)


 - 参数：

   - item 要添加到列表末尾的元素。


 - 说明：

   - list.app end 函数在原地修改列表，
即



它会直接修改调用它的列表， 而不是创建— 个新列表。


##### 2 Python 变 量

**2.5** 列表类型变量


2 列表变量 常用方法（函数）


(2) insert 方法


置

 - 语法： li st.insert(index, item)


 - 参数：

   - Index 设置要插入元素的位置。

   - Item 设置要插入到列表中的元素。


##### 2 Python 变 量

2.5 列表类型变量


2 列表变量 常用方法（函数）


(3) remove 方法


 - 功能： 删除列表中某个值的第—个匹配
项

 - 语法： I i st.remove(item)


 - 参数：

   

##### 2 Python 变 量

**2.5** 列表类型变量


2 列表变量常用方法（函数）


(4) p op 方法


 - 功能：  删除列中一个元素（默认最后一个元素），并返回该元素至。


 - 语法： li st.pop(index)


 - 参数：

   - index 设置要删除的元素的索引位置。如果不传入任何参数， 则 默认删除列表的录后— 个元素。


##### 2.2 Python 变 量

2.2.5 列表类型变量


2 列表变量 常用方法 （函数）


( 5) count 方法


 - 功能： 统计某个元素在列表中出现的次
数

 - 语法： I i st.count( item )


 - 参数：

   - item 设罣需要统计出现次数的元素。


##### 2 Python 变量

2.5 列表类型变量


2 列表变量常用方法（函数）


(6) sort 方法




- 功能： 对原列表进行排序， 如果指定参数，
则
. i 吾法： li st.sort(k ey=None, reverse=

- 参数：
False)



使用比较函数指定的比较函数。




 - key 可选参数 ， 用于指定排序时要使用的键（函数）。

 - reverse 可选参数 ， 用于指定排序顺序， 如果为 True 则按照降序排列， 否 则 按 照

- 升如果不指定这两个参数序排列。 ， 按 照默认顺序（即升序）排序。
则



按 照默认顺序（即升序）排序。


##### 2 Python 变 量

**2.6** 元组类型变量

   元组 (tuple) 与列表 (list) 类型一样，可以存储包含多个元素的变量。与列表不同，元组
一旦创建就不可以进行对内部元素进行追加、删除操作 。

   元组变量通过圆括号“ ()” 进行创建。为了区别元组的圆括号“ ()” 与运算符的圆括号
“ ()” 进行区别当元组内部只有一个元素时候，需要在该元素后加上一个逗号“,” 。


##### 2 Python 变 量

**2.7** 集合类型变量


1 集合变量


   - 在 Python 中，集合类型变量需要通过大括号的 {} 来创建，也可以通过
Python 内置函数 set 传入一个列表或元组进行创建。


##### 2 Python 变 量

2.7 集合类型变量


2 集合变量常用方法（函
数）

   - set.add(item) set.add(item): 向集合中添加一个元素，如果元素已经存在，
则不会进行任何操作。

   - set.update(iterable): 向集合中添加一个可迭代对象中的所有元素。

   - set.remove(item): 从集合中删除一个元素，如果元素不存在，则引发
KeyError 异常

   - set.discard(item): 从集合中删除一个元素，如果元素不存在，则不会进行任何
操作。

   - set.pop(): 删除并返回集合中的任意一个元素，如果集合为空，则引发
KeyError 异常

   - set.clear(): 从集合中删除所有元素。 set.copy(): 返回集合的一个副本。

   - 。


##### 2 Python 变 量

2.8 字典类型变量


1 字典变量


  - 在 Python 汇总，字典类型变量需要通过大括号 {} 进行创建，字典内部每个元素是以
key:value 的形式存放，例如 {“stock code”:“SH.600000”} 表示字典中有   - 个键为 stock
code, 对应的值为 SH.600000.


##### 2 Pyt hon 变 量

2.9 字典变量的常用函数与方法

   - dict.keys(): 返回一个包含字典所有键的列表。

   - dict.values(): 返回一个包含字典所有值的列表。

   - dict.items0): 返回一个包含字典所有键值对的列表，每个键值对表示为一个元组。

   - dict.get(key, default=None): 返回给定键的值，如果键不存在，则返回默认值。

   - dict.pop(key,default=None): 删除并返回给定键的值，如果键不存在，则返回默认值。

   - dict.popitem(): 删除并返回字典中的任意一个键值对， dict.clear(): 删除字典中的所有键值对。

   - dict.update(other dict): 将另一个字典中的键值对添加到当前字典中。

   - dict.fromkeys(iterable,value=None): 返回一个新字典，其键为可迭代对象中的元素，值为指定的值。

   - dict.setdefault(key, default=None): 返回给定键的值，如果键不存在，则将给定键和默认值插入到
字典中。


#### 3 流程控制

3.1 逻辑判断

在 Pyhont 中，逻辑判断涉及布尔类型变量与逻辑运算符两个要素。其中，逻辑运算符包括
and( 与 ) 、 or( 或 ) 和 not( 非 ) ，这三种逻辑运算符使用方法如下 :

  - and: 条件 1and 条件 2 ，如果条件 1 和条件 2 都是 True 时，返回 True 否则返回 False 。

  - or: 条件 1or 条件 2 ，如果条件 1 和条件 2 都是 False 时，返回 False ，否则返回 True 。

  - not:not 条件 1 ，如果条件 1 是 True 时，返回 True ，否则返回 False 。

  - and( 与 ) 、 or( 或 ) 和 not( 非 ) 这三种逻辑运算符可以一起使用, 一般通过使用括号来标记运算优先
级。


#### 3 流程控制

3.2 if 判断

1. if …else 单—条件判断


   - If…else 语句是 Python 编程中录常用的进行判断的语法结构。通过 i f … else 条件判断可以设置
触发条件， 并执行对应条件的代码与功能。例如金融量化策略在判断是否满足入场交易条件
时 ， 可以 通过 i f …else 进行判断。


2 elif…else 多条件判断


#### 3 流程控制

3.3 循环语句

1. for 循环 : 金融量化分析常常需要多次执行相同的操作，例 如， 需 要针对多个数据文件进行相


#### 4 函数

4.1 定义函数与调用函数


   - 在调用之前，必须事先进行函数定义。由于每个函数执行特定功能，在定义函数之前需要


#### 4 函数

4.2 函数的参数


1 形式参数的顺序


   - 形式参数的顺序形式：参数为多个时，调用函数所传递的实际参数顺序应该与形式参数的顺序
一致。但是，如果直接将实际参数赋值给形式参数关键字，则形式参数的顺序可以与函数调用
时实际参数的顺序不一致。


一种是基于字典的可变参数，通常以 **kwargs 表示。


#### 4 函数

2.4.3 lambda 匿名函数

lamb da 匿名函数是—种可以在代码中临时定义并调用的小型函数。 lamb da 匿名函数通常是一行代
码， 并且只被使用一次。

 - 语法： lamb da 参数列表 函数体


 - 说明：

   lamb da 匿名函数的参数列表可以包含零个或多个参数。如果是多个参数 ， 可以用逗号分隔。

   lamb da 匿名函数体通常是—个表达式， 用于计算并返回的函数结果。


#### 4 函数

4.4 Pyt h on 高阶函数


1. map 函数


·功能： 将一 个可迭代对象中每个元素应用一个函数， 并返回一 个新的可迭代对象， 返回的可迭代
对 象包含参数函数的每— 个返回值


 - 语法： m ap(functio n, iterab le)


 - 参数：


   - functio n 需要多次执行的函数。

   - iterable 可迭代对象。 iterable 中的每个元素应用— 个函数， 并返回— 个新的可迭代对象，

可迭代对象包含新的 functio n 函数运算的全部返回值。


#### 4 函数






#### 4 函数


 - 语法：


  - reduce(functio n, iterab le[, initializer])


 - 参数：




- function
：

- iterable

- init ializer



用千指定要应用的 函数。
可迭代对象， 用于指定需要逐 一处理的元素集。

可选的初始值， 用千在可迭代对象为空时作为结果返回。


#### 5 模块应用

5.1 模块的安装、卸载与调用


1. pip 命令自动安装


#### 5 模块应用

5.1 模块的安装、卸载与调用


3 模块的卸载


   - 当需要卸载模块时， 可以使用在在 Anaconda Pro m p t 命令窗口 （或 window 系统自带的 Dos 命
令窗 口） 输入命令 “ pip uninstall 模块名称”即可。


4 模块的调用


   - 可以使用两种方法调用 Pyt ho n 模 块， — 种是直接使用 mport 关键字导入模块， 第 二种是可
以使用


#### 5 模块应用










#### 思考题：

1 列表变量有哪些方法和函数？这些方法和函数的应用场景是什么？


2 字典变量有哪些方法和函数？ 这些方法和函数的应用场景是什


么？ 3 列表与元祖的相似点与不同点是什么？两者的应用场景有


何差异？


