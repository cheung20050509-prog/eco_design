# 3第3章.pptx

## Slide 1

第3章 金融量化分析工具准备－－Pan das基础

Pandas 简介
Series对象
DataFrame对象

Pandas 金融量化分析应用
Pandas 数据可视化

## Slide 2

1	Pan d as简介

1.1 Pan das主要优势
可处理格式多样的数据源文件。Pandas可从多种格式的数据文件中提取数据， 例如， EXCE L、 CSV、SQL、JSON、HTML、TXT、HDF5、XM L、SAS等文件。
函数功能丰富、调用简便。Pandas提供了丰富的数据处理方法与函数， 这些方法与函数涵盖数 据清洗、数据类型转换、数据分组与聚合、数据合并与连接、数据可视化等功能， 这使Pandas  具备可以处理复杂数据分析工作的能力。
功能扩展性高。Pandas与Numpy、Matplotlib、SK-learn等专业应用模块高度集成， Pandas模 块方法可以与上述模块方法直接结合使用， 实现功能扩展、优势互补。
数据处理运算速度高。由于Pandas模块底层基于高效科学计算模块Numpy实现， 自 身内部采取 向量化操作模式且支持并行计 算， 因此， Pandas在处理金融量化分析数据具备强大的高速运算 能力。

## Slide 3

1	Pan d as简介

1.2 Pan d as主要功能
数据获取。Pandas提供 了从不同格式的文件中（例如， EXCE L、CSV、SQL、JSON、HTML、TXT、 HDF5、XM L、SAS等格式文件）加载数据的方法。
数据存储。在获取外部数据后， Pandas内部 以Series对象或DataFrame对象方式进行存储， 这两 种对象数据均具有索引属性， 可 进行快速查询、定位功能。
数据清洗。Pandas通过fillna、dropna 、dup licated 、drop_d uplicates等方法进行数据清洗。
数据处理。
数据选择与过滤 数据统计与聚合
数据合并与拼接
数据结构变形与重构 时间序列处理

## Slide 4

1	Pan d as简介

1.3 Pan d as的底层结构
Pandas 的底层结构是由Numpy数组和自定义数据类型组成的， 其中， Series对象和DataFrame对 象是Pandas 自定的两类数据结构
Series对象是—种带有标签的—维数组， 可以存储任意类型的数据。每个数据点都有—个与之 对应的标签	—般称为索引。Series可以被视为— 个由索引和值组成的字典。
DataFrame对象是由多个Series对象组成的表格型数据结构， 可以被视为— 个二维数组， 其中 每—列可以是不同的数据类型。DataFrame具有行索引和列索引， 可以进行行列操作， 支持多种 类型的数据操作。

## Slide 5

2	Series对象

3.2.1创建Series对象
Series函数
功能： 通过Series函数创建Pandas 中Series对象。
语法： p andas.Series(data ,	*args)
参数：
data	列表、Num Py数组、字典、标量等类型数据，必	选参数， 用于设置生成Series对象的数据
index	列表、Num Py数组、标量或其他可迭代对象类型数据， 可选参数， 表示Series对象的索引。
dtype	字符串类型数据， 可选参数， 设 罣Series对象的数据类型， 可以是Num Py数据类型或 Pyt ho n内置数据类型。
copy	布尔类型数据， 可选参数， 设 置是否复制数据， 默认为False,	表示直接引用原始数据。
name	字符串类型数据， 可选参数， 设 罣Series对象的名称， 可以通过Series.name调查看该属性。

## Slide 6

2	Series对象

2.1	创建Se ries 对象
2 以列表数据创建Series对象

以列表变量创建Series对象是创建Series对象录为方便 的—种方式。由于列表变量内部可以包含 丰富的数据类型， 因此， 使用列表变量创建Series对象， 可以解决绝大部分的Series对象创建问 题。
3 以字典数据创建Series对象

当字典类型数据作为Series对象生成的输入数据时， 如果没有传入索引index参数，则	会按照字典 的键来构造索引， 如果传递了索引index参数，则	根据索引index参数创造Series对象。

## Slide 7

2	Series对象

2.2	访问Seri es对象数据
通常可以通过两种方式访问Series对象数据， — 种方法利用数据的位置索引访问， 另—种方法利 用索引标签访问
利用数据的位置索引访问Series对象数据	与调取列表类型数据 (list)—样， Pandas 的Series对 象数据也可以通过Series内部数据元素自身的下标进行访问。下标索引从 0 开始计数，以此类推， 可以获得Series 序列中的任何位器的元素。与列表数据—样， Series对象数据也支持切片操 作。
利用数据的索引标签访问Series对象数据	与字典diet 类型数据相似， Series对象索引标签与字典 对象的键key相对于， Series对象数据元素与字典对象的值value相对于， 即Series对象数据通过 键 (index索引标签）来访问值 (Series对象数据）。

## Slide 8

2	Series对象

2.3 Seri es对象常用属性
Series对象的常用属性主要包括axes、values、index、dtyp e、shape、name、size等， 在进行数
据分析与处理过程中常常需要调取这些属性信息。

## Slide 9

2	Series对象

2.4	Seri es常用函数
(1)	head和tail函数

功能： 分 别返回Series对象的前n行和后n行数据。
语法： Series.head(n= 5)
·参数：
n	指定返回Series对象的前n行， 默 认 为5

## Slide 10

2	Series对象

2.4 Serie s 常用函数
(2)	describe 函数

功能： 返 回 Series对象的统计信息， 如计数、平均值、标准差、最小值、最大值等。
语法： Series.describe(percenitles=None. include=None. exclude=None)
参数：
percentiles	指定要显示的百分位数列表， 类 型 为列表。默认为[.25, .5, .75]， 即 25%、 50％和 75％的 百 分 位 数 。 如 果 要 自  定 义 百 分 位 数 ， 可 以   传 入 — 个 包 含 浮 点 数 的 列 表 ， 例  如 [.1,. 2, .3, .4, .6, .7, .8, .9]。
include	指定要包括的数据类型列表， 类 型 为字符串或列表。默认为None,	表示包括所有数据类型。如果 要仅包括某些数据类型， 可以  传 入 — 个 包 含 数 据 类 型 名 称 的 字 符 串 或列表 ， 例  如  num     ber'  或 ['number'
'd atet ime' ]。
exclude	指定要排除的数据类型列表， 类 型 为字符串或列表。默认为None,	表示不排除任何数据类型。如 果要排除某些数据类型， 可以  传 入 — 个 包 含 数 据 类 型 名 称 的 字 符 串 或列表 ， 例  如 o bject	或 ['o bject'
·c atego ry'］。

## Slide 11

2	Series对象

2.4	Seri es常用函数
(3)	value_count s函数

功能： 返回Series对象中每个值的计数。
.	语法： Series.value_count s(normalize=False, sort=True, ascending =False, bins=None, dropna=True)
·参数：

则按降序排列。默认为False。 要使用的箱数。

no rmalize 如果设罣为True,	返回每个唯—值的相对频率而不是计数。默认为False。
sort	如果设罣为True,	则按计数值对输出进行排序。默认为True。
ascending	如果设置为False,
bins 只对数字数据有效， 指定
dropna 如果设罣为False,	则包括缺失值。默认为True。

## Slide 12

2	Series对象

2.4	Seri es常用函数
(4)	sort_values函数

功能：     按照值的大小对Series对象进行排序。
.	语法： Series.sort_values(axis=O, ascending = True, inplace=False, kind= 'quicksort ', na_p os ition='last',  igno re_in d ex=False, key=None)
·参数：
axis	指定排序的轴。对于 Series 对象， 只 能为 0。默认为 0。
ascending	指定排序的顺序。如果为 True,	则按照值的升序排列， 如果为 False,则按照值的降 序排列。默认为 True。
inplace	指定是否直接修改原始 Series 对象。如果为 True,则直接修改原始 Series 对象， 如果为 False,则返回—个排序后的新的 Series 对象。默认为 False。

## Slide 13

2	Series对象

2.4	Seri es常用函数
(5)	map函数

功能： 根据指定的函数对 Series对象中的每个值进行映射。
语法： Series.map(arg, na_actio n= None)
·参数：
arg  要应用于Series对象内每个元素的函数，也	可以是—个字典、Series或其他可迭代对象。如果 是字典或Series,	则将Series中的每个元素映射到字典或Series中的相应值。
na_actio n 指定如何处理缺失值。可选参数为igno re或'raise'， 默认为N o n e。如果为igno re'， 则 将缺失值视为无操 作， 否 则将引发异常。

## Slide 14

2	Series对象

2.4	Seri es常用函数
(6)	apply函数

功能： 对Series对象中的每个值应用指定的函数。
.	语法	Series.apply(func, convert _dt yp e=True, args=())
·参数：
func 要应用于每个元素的函数或可调用对象。
convert _dt yp e  如果为True,	则尝试将结果转换为原始Series对象的数据类型。默认为True。
args 传递给函数的其他参数， 以 元组形式提供。例如， args=(l O,）表示将10作为函数录后—个参 数参与运算， args=(l 0,11)表示将10与11作为函数录后两个参数参与运算。

## Slide 15

3	DataFram e对象

3.1 DataFrame对象数据存储结构
数据区域 (Data)存储数据本身， 通常是—个二维的NumPy数组 (numpy.ndarray) ， 每—列可以有不同的数据类型。
行索引 (Index)用于唯—标识每—行， 通常是—个pd.Index对象或其子类，例如
pd.Rangelndex。
列索引 (Columns)用于唯—标识每—列， 通常是—个pd.Index对象或其子类，例如
pd.Rangelndex或p d.M ultiIndex。
轴标签 (Axes):pd.DataFrame对象同时具有行和列两个轴， 因此有两个轴标签，分别对应行 索引和列索引。
此外， p d .DataFrame还有—些其他的属性，例如数据类型 (dtype)、形状 (shape)、内存信
息 (memory_usag e(）)等。

## Slide 16

3	DataFram e对象

3.2 创建Dat aFram e对象
Data Frame 函数

功能： 创建Pand as中Data Frame 对象的构造函数。
语法：      pand as.Data Frame(data,	*args)
参数：
data	字典、二维数组、Series 等类型数据， 用于设罣构建Data Frame 的数据来源。data 数据可 以是结构化的， 也 可以是非结构化的， 如JSON、CSV、非不关 系型数据库中的数据等。如果数 据为字典 类型，则	字典中的每个键对应若Data Frame 中的—个列， 每个键的值对应若该列中的数 据。如果data 数据为二维数组，则	数组中的每—行对应若Data Frame 中的—行， 每—列对应若—  个Data Frame 的列。
columns	列 表或元组类型数据， 设 置Data Frame 的列名， columns 长度必须与data 中每—行数据 的长度相同。如果不指定列名，则	默认为0到n-1 的 数字序列。

## Slide 17

3	Dat aFr am e对象

3.2 创建Dat aFram e对象
2	创建空的Dat aFram e对象
当不确定pand as.Data Fra me 数据内容时	可以先通过pand as.Data Fra me (）函数创建一个空的

Data Fra me 对象或者创建—个只包括列名的空Data Fra me 对象。在获取数据之后，再	将数据

保存至Data Fra me 对象内。
3	使用列表创建Data Fame 对象

可以使用—维列表或嵌套列表来创建一个 Data Fra me 。
 4 使用列表嵌套字典创建 Data Frame

列表嵌套字典可以作为输入数据构建Data Fra me 对象，默

认 情 况下	，字典	的 键被用作列名。

如果列表中字典元素键值对出现不一致时， 即 列 表内嵌套的字典的 key 无法找到对应的
va lue ,	将使用 Na N 代替 。

## Slide 18

3	DataFram e对象

3.3	Dat aFram e对象常用属性

DataFrame对象的属性主要包括：
columns:	返回数据框中的列标签。
index
values:
shape
dtypes

返回数据框中的行标签。 返回数据框中的值
返回数据框的维度。
返回数据框中每个列的数据类型。

## Slide 19

3	DataFram e对象

3.4	Dat aFram e列操作方法
1 访问Data Frame 对象数据列

分析Data Frame 对象内部数据， 需 要访问Data Frame 对象内部数据列。Pand as提供了以下几种访问 Data Frame 对象数据列的方式

可以通过列名选取一列， 此方法适用于列名是一个有效的Pytho n 变量名的情况。

如果列名是有效的Pytho n变量名， 并且不包含空格等特殊字符

可以使用属性

列名选取
属性选取 选取。

切片选取多列	可以通过使用iloc

属 性，以切片方式选取多列。

列索引	可以使用loc属性结合列名来选取数据列。
布尔索引	可以使用loc	数据结	合布尔索引选取多列。

## Slide 20

3	Dat aFr am e对象

3.4	Dat aFram e列操作方法
2 添加Data Frame 对象数据列

在pand as.Data Frame 对象中， 常用的添加数据列的方式有以下几种

直接赋值	通过pand as.Data Fra md 实例对象” d［f 新 列 名 ']”进 行 赋 值

可 以 直 接 添 加 一 列 数 据 。

，新	列 名 ］”进 行  赋 值 ， 在原有的数据框基础上添加一列数

使 用loc	方 法	通 过 直 接 对	'df.loc[
据。

通过df.ass ig n（新列 名＝列 表 数 据）， 将 列 表 数 据 添 加 至 新 列 名 对 应列内。需

使用assig n方法
要注意的是

## Slide 21

3	DataFram e对象

3.4 Dat aFram e列操作方法
3 删除Data Frame 对象数据列
使用del语句 直接通过del关键字即可删除指定的Data Fra me 对象的数据列。例如， del  df['price  ' ]表示删除price列 数 据  。
使用pop方法pop方法是pand as.Data Fra me 对象的一个方法， 可 以 删 除 指 定 的 一 列 数 据 ，  并 返 回 所 删 除 的 那 — 列 数 据 。
使用dro p方法	dro p方法也是pand as.Data Fra me 对象的一个方法。与pop方法不同， drop  方法除了需要指定删除的列名称， 还需要设置参数axis =l （表示按列操作），  且执行 drop  方法返回的是巳删除指定列后的Data Fra me 对象。例如， df.d ro p('p rice	' , axis =l )表示删除 price	列 数 据 。

## Slide 22

3	Dat aFr am e对象

3.4 DataFram e列操作方法
DataFrame对象数据排序

在序列分析中， 常常会有排序的需求。在pandas.DataFrame中， 可以使用多种方法对数据进行排序，  其中常用的方法使用sort_values方法。sort_values方法可以按照指定的列或多列对数据进行排序， 可 以指定升序或降序排列。
sort_values方法有主要的参数包括
by	指定按照哪些列进行排序， 可 以 是 单个列名， 也 可 以 是 多 个 列 名 组 成 的 列 表 。 例 如，
b y = ' p r i ce'或by= ['code', 'price'］。
axis	指定按照行或列进行排序， O表 示按照行进行排序， 1 表 示按照列进行排序， 默 认 为0。
ascending	指定是否按照升序排列	默认为True,	即按照升序排列， 如果设置为False ,	则 按照降序排列。

inplace	指定是否在原数据框上进行排序	默认为False ,	即不在原数据框上进行排序， 如

果设置为True,
ignore_ind ex

则在原数据框上进行排序。
指定是否忽略索引， 即 将 排 序 后 的 结 果 重 新 生 成 索引。默认为False ,即不忽

略索引， 如果设置为True,	则将排序后的结果重新生成索引。

## Slide 23

3	DataFram e对象

3.4 Dat aFram e列操作方法
DataFrame数据查询

Pan das提供了多种数据查询的方法， 常用的数据查询包括
使用query方法：可以按指定的查询条件表达式或函数进行查询，该方法的主要参数包括
expr：必需参数表示要查询的条件表达式。例如， ＇p r i ce > 6表示df['price > 6']。
inplace： 可选参数表示是否在原数据框上进行修改默认为False ,即不修改原数据框。

## Slide 24

4	Pand as金融量化分析应用

主 要的统计

统计计算

Pa nd as可以方便的通过各种统计函数对Data Fra me 或Se ries	对象进行统计计算， 函 数 包 括 '	min、 max 、 me an、 med ian、 var、 std 、mode 等。
累积计算

在金融量化分析中，经	常需要处理累计计算问题， 如在计算最大回撤率时，就需 要计	算 截 止每一行收益率累计最大值。Pa nd as为Se ries对象提供了几种常用的累积计算方法， 如 cumsum 、 cum prod、 cummax 与cum min等。
3.4.3 获取CSV文件数据

如txt、

处理数据， 首先需要获取数据。Pa nd as可以方便的从多种格式数据文件读取数据，例 exce l、ht m| 、 jso n、 csv等格式文件保存的数据均可被Pa nd as读取。

## Slide 25

5	Pand as数据可视化

折线图的具体细节可以通过设置参数完成。

折线图
Pa nd as通过调用plot 函数可以制作折线图，
直方图

Pa nd as可以通过两种方法绘制直方图， 一种是直接通过plot	方法绘制， 另一种是通过hist 方 法完成。

## Slide 26

5	Pand as数据可视化

折线图的具体细节可以通过设置参数完成。

折线图
Pa nd as通过调用plot 函数可以制作折线图，
直方图

Pa nd as可以通过两种方法绘制直方图， 一种是直接通过plot	方法绘制， 另一种是通过hist 方 法完成。

## Slide 27

5	Pand as数据可视化

1. 折线图
Pa nd as通过调用plot 函数可以制作折线图，折线图的具体细节可以通过设置参数完成。
2. 直方图
Pa nd as可以通过两种方法绘制直方图， 一种是直接通过plot方法绘制， 另一种是通过hist 方 法完成。

## Slide 28

思考题：

1	如何将CSV文件数据存储入SQLite数据库文件？
Pandas 模块中Series对象与DataFram e对象之间的关系是什么？ 两者之间存在哪些相同点与不同点？
DataFram e对象列操作方法有哪些？ 这些操作方法有何用途？

