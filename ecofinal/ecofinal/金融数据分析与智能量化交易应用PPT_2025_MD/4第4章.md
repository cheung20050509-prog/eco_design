# 4第4章.pptx

## Slide 1

第4章 金融量化分析工具准备－－  N u m   p y 基础

Numpy简介
Numpy对象创建
Numpy数组数据访问

Numpy数组操作
Numpy模块的主要函数
Numpy随机数处理

## Slide 2

1	Numpy简介

1.1 Numpy主要优势
提供了强大的N 维数组处理方法。Numpy的核心是多维数组对象(ndarray)，它可以通过矢量化、索引和广播概念实现高效存储和处理大量数据，而且支持各种数组操作，例如切片、索引、转置、合并等等。
提供广播实现功能。Numpy通过广播功能可以让不同形状的数组进行运算，而无须进行显示的复制数据，这极大地简化了代码的编写。
提供了丰富的数值计算以及统计函数。Numpy 提供了全面的数学函数，包括随机数生成器、线性代数、傅里叶变换等函数具备与其他科学计算模块的高等级兼容性。Numpy与其他科学计算模块(例如Scipy、Pandas等)结合使用，可以很方便地进行数据处理与科学计算等。
提供了高性能计算能力。由于Numpy的核心代码是优化良好的C代码，因此Numpy既体现了Python的灵活性，又可以发挥C语言代码高速执行的优势。
提供了多平台使用解决方案。Numpy支持广泛的硬件和计算平台，并且可以很好地与分布式、GPU和稀疏数组模块配合使用·

## Slide 3

1	Numpy简介

1.2 Numpy主要功能
以数组形式保存、处理数据：Numpy的核心是ndarray(N-dimensionalarray，多维数组)对象，Numpy通过ndarray对象为各领域数据提供了便捷的多维数组存储方式，这种数组结构可以进行各类操作，包括切片、索引、转置、合并等操作。
数学运算：Numpy提供了各种数学函数，包括三角函数、指数函数、对数函数、幂函数、复数函数等等。Numpy也提供了各种线性代数运算函数，可以方便的进行各类线性运算，例如向量和矩阵的乘法、矩阵的逆和行列式计算等。此外，Numpy也提供了各类统计函数，可以计算平均值、方差、标准差、最大值、最小值等各类统计量。
生成随机数：Numpy提供了各种随机数生成函数，可以生成不同分布的随机数，例如正态分布、均匀分布、泊松分布等等。

## Slide 4

1	Numpy简介

1.2 Numpy底层结构
NumPy的底层结构的核心是ndarray(N-dimensional array，多维数组)对象。ndarray对象是一个由同，以实现计算效率的大幅提升。ndarray类型元素组成的多维数组，，其中每个元素占用固定大小的内存对象有以下几个重要属性:
shape:ndarray的形状，用一个整数元组表示，每个元素表示数组在每个维度上的大小。例如，一个二维数组的shape属性为(n,m)，其中n表示数组在第1维度上的大小，m表示数组在第2维度上的大小。具体地说，对于一个一维数组，它的shape只有一个元素，表示数组的长度;对于一个二维数组，它的shape有两个元素，分别表示数组在第1维度和第2维度上的大小,对于一个三维数组，它的shape有三个元素，分别表示数组在第1维度、第2维度和第3维度上的大小;以此类推。
dtype:ndarray中元素的数据类型，NumPy支持的数据类型包括布尔型(bool)、整数型(int、int8、int16、int32、int64)、无符号整数型(uint、uint8、uint16、uint32、uint64)、浮点型(foat、float16、foat32、float64)、复数型(complex、complex64、complex128)。
ndim:ndarray对象的维数(也称为秩)，即数组的轴数。

## Slide 5

2	Numpy对象创建

2.1 创建ndarry对象
Numpy通过array函数创建ndarray对象。
语法:numpy.array(object, dtype=None, copy=True, order='K', subok=False, ndmin=0)
参数:
object:序列、列表、元组、数组或其他序列类型，该参数为必须设置的参数，用以指定array创建的数据来源。
dtype:可选，数组的所需数据类型，
copy:布尔类型数据，可选，设置是否需要复制数据，默认为True。
order:字符串类型数据，可选。

## Slide 6

2	Numpy对象创建

2.2 创建特殊ndarry对象
1.empty方法
功能:创建指定形状和数据类型的未初始化数组。返回指定形状和数据类型的未初始化数组。未初始化数组的内容是随机的，可能包含垃圾值。
语法:numpy.empty(shape,dtype=float, order=‘C”)
2.zeros方法
功能:用于创建指定形状和数据类型的全零数组。语法:numpy.zeros(shape, dtype=float, order='C’.
3.ones方法
功能:用于创建指定形状和数据类型的全1数组。语法:numpy.ones(shape, dtype=float, order='C”)

## Slide 7

3.1 索引
1.一维数组
单一元素的索引访问当访问一维数组单一元素时，仅需要将被访问元素的索引号写入0内即可。
2.一维数组多元素的索引访问
当访问一维数组多元素时，则需要将被访问元素的索引号以列表形式写入几即可
3.多维数组单一元素的索引访问
当访问多维数组单一元素时，需要将该元素的在每一个维度的索引号写入0内即可，各维度的索引号之间以逗号”,"分割。
4.多维数组多元素的索引访问
当访问多维数组多元素时，需要将每一元素的在每一个维度的索引号写入以列表形式保存，并逐个写入多维数组后的内即可，每个元素的各维度索引号列表之间以逗号“,"分割。

3 Ndarry数组数据访问

## Slide 8

3.2 切片
1.获取数组指定行数据
如果需要访问二维数组的指定行数据，仅指定ndarray对象的行索引号即可
2.获取指定列数据
如果需要访问二维数组的某列数据，则需要将“:列索引号”填入ndarray对象后的I内即可
3.获取指定行以后所有数据
如果需要访问指定行以后所有的数据，ndarray对象后的填入“行索引号:”即可。

3	Ndarry数组数据访问

## Slide 9

3.2 切片
4.布尔切片Numpy
可以通过布尔数组进行切片。布尔索引通过布尔运算(例如:比较运算符)来获取符合指定条件的元素的数组，
5.花式切片
NumPy的花式切片fancyindexing)是指使用整数数组来进行索引，从而获取数组中指定位置的元素。花式切片可以用于获取数组中的任意一组元素，可以用于选择、修改和重新排列数组中的元素。

3 Ndarry数组数据访问

## Slide 10

4.1 修改Ndarray数组形状
1.reshape方法
功能:用于重新定义ndarray数组的形状。该函数将给定的ndarray数组转换为指定形状的新ndarray数组，而不改变原始数据的顺序或值。
2.flatten方法
功能:用于将一个ndarray数组转换为一维数组。该函数返回一个展开的一维ndarray数组，其中包含原始ndarrav数组中的所有元素。返回的一维数组是原始数组的副本，所以对返回的一维数组所做的任何修改都不会影响原始数组。
3. transpose方法，
功能:用于对ndarray数组进行转置操作。转置操作会将ndarrav数组的行和列交换，也就是将行变为列，列变为行。

4 Ndarry数组操作

## Slide 11

4.2 Ndarray数组的合并
Numpy通过concatenate函数可以将不同的ndarray数组按照指定轴进行合并
语法:np.concatenate((a1,a2..),axis=0.out=None).
参数:
a1,a2,…:需要拼接的数组序列。
axis:整数或者None，用于设置拼接的轴向。默认值为0，表示在第1个维度上拼接。如果设置为None，则把所有数组视为一维数组进行拼接。
out:ndarray类型数据，用于设置输出结果的数组，如果没有指定，就会新创建一个数组来存储结果

4 Ndarry数组操作

## Slide 12

4.3 Ndarray数组的分割
Numpy可以通过split函数实现ndarray对象数据沿着指定的轴进行分割为多个子数组
语法:numpy.split(a,indices _or_sections, axis=0)
参数:
a:需要被分割的数组。
indices or sections:指定分割的位置。可以是一个整数表示要将数组分成几个等分，也可以是一个列表或元组，表示每个分割点的位置。注意，这里的位置指的是分割轴上的索引，而不是元素个数。具体来说，当indicesor sections是一个整数N 时，numpy.split函数将数组分割成N个相等的子数组。如果indices_or_sections是一个整数数组，那么它将指定每个要创建的子数组的结束索引(不包括该索引)。例如，如果indices orsections是[2,4]，那么numpy.split函数将数组分割成三个子数组，分别包含原数组的前两个元素、第3个到第4个元素、第5个到最后一个元素。
axis:表示沿着哪个轴进行分割。默认值为0，表示沿着第0轴进行分割。如果ary是一个二维数组，则axis=0表示沿着行的方向进行分割，axis=1表示沿着列的方向进行分割。

4 Ndarry数组操作

## Slide 13

4.4 Ndarray数组数据的删除
1.删除单一元素
可以使用delete函数删除ndarray对象中的数据，.语法:numpy.delete(arr, obj,axis=None).
2.删除指定行、列数据
使用delete函数，可以指定要删除的轴，例如删除第1行。。3.切片删除数据
使用delete函数，也可以将obi参数设置为切片或花式索引形式，实现删除指定多组数据功能，例如删除第1列与第3列数据。

4 Ndarry数组操作

## Slide 14

4.5 Ndarray数组数据
添加添加整行或整列数据，可以使用append函数实现:
语法:numpy.append(arr,values,axis=None)
参数:
arr:要操作的ndarray对象。
values:要添加的元素，可以是标量或ndarray对象
axis:指定要添加元素的轴。如果不指定，则将数组展开为一维数组后添加元素

4 Ndarry数组操作

## Slide 15

5 Ndarry模块的主要函数

5.1 统计类函数
Numpy可以通过内部统计函数，方便的进行各类统计操作。Numpy模块中常用的统计函数包括
mean():求取数组的平均值。
median():求取数组的中位数
std():求取数组的标准差。
var():求取数组的方差。 
min():求取数组中的最小值
max():求取数组中的最大值，
sum():求取数组中元素的总和。
percentile():计算百分位数。
histogram():统计数组的直方图
corrcoef():计算相关系数矩阵。
cov():计算协方差矩阵。

## Slide 16

5.2 线性代数类函数
Numpy模块包括丰富的线性代数类函数，可以方便的进行线性相关计算。Nmupy模块的线性代数运算的函数包括:
numpy.dot():计算矩阵乘积。
numpy.transpose():计算矩阵的转置。
numpy.trace():计算矩阵的迹。
numpy.linalg.det():计算矩阵的行列式
numpy.linalg.inv():计算矩阵的逆。
numpy.linalg.eig():计算矩阵的特征值和特征向量。
numpy.linalg.svd():计算矩阵的奇异值分解。
numpy.linalg.norm():计算矩阵的范数

5 Ndarry模块的主要函数

## Slide 17

5.3 排序与筛选函数
Numpy为ndarray对象内部数据的排序、筛选提供了对应函数与方法，如sort、argsort、argmax、argmin与where函数等。
1.sort函数
Numpy可以通过sort函数实现基本排序，返回一个按指定轴排序后的数组副本。
语法:numpy.sort(a,axis=-1,kind=None, order=None)·
参数:
a:要排序的数组。
axis:排序沿着指定的轴进行。默认值为-1，表示沿着最后一个轴排序。
kind:指定排序算法，可以取值为'quicksort、'mergesort、heapsor'中的一个。默认为'quicksort’。
order:如果数组包含字段，则指定要排序的字段。

5 Ndarry模块的主要函数

## Slide 18

5.3 排序与筛选函数
2.argsort函数sort函数可以返回排序后的数组，如果希望获取沿给定轴排序后的数组索引，则可以通过argsort函数实现。
语法:numpy.argsort(a,axis=-1,kind=None, order=None)
3.argmax和argmin函数argmax和argmin函数分别沿给定轴返回数组最大和最小元素的索引
语法:numpy.argmin(ndarray,axis)及numpy.argmax(ndarray, axis
4.where函数where函数根据设定的条件返回一个数组，其中包含满足给定条件的输入数组中对应元素的索引。
语法:numpy.where(condition,x=None,y=None)

5 Ndarry模块的主要函数

## Slide 19

6.1 Numpy处理随机数问题的优势
Numpy在处理随机数问题上具有非常大的优势，主要体现在以下方面:
一致性:NumPv提供了许多随机数生成器，这些生成器使用了相同的接口，因此在使用时非常一致。这种一致性使得代码更易于编写、调试和维护。
可重复性:NumPy的随机数生成器是伪随机的，但可以设置随机数种子，使得随机数序列可以重复生成。这种可重复性在测试和调试时非常有用。
灵活性:NumPy提供了多种随机数生成器，可以生成不同分布、不同范围和不同形状的随机数。这种灵活性使得NumPy在各种数据分析和科学计算任务中都非常有用。
多维数组支持:NumPy可以生成多维随机数数组，这种多维数组在处理图像、音频和视频数据等方面非常有用。
算法丰富:NumPy提供了多种生成随机数的算法，包括梅森旋转算法(MersenneTwister)、互相关移位反馈寄存器算法(xorshift)、超几何分布等。这种算法丰富性使得NumPy可以满足各种随机数生成的需求。

6 Ndarry随机数处理

## Slide 20

6.2 随机数生成
numpy.random.rand():生成一个[0,1)之间均匀分布的随机数，可以生成指定形状的随机数数组。
numpy.random.randn():生成一个标准正态分布(均值为0，方差为1)的随机数，可以生成指定形状的随机数数组。生成的随机数是在[0.0,1.0)之间均匀分布的随机数组。
numpy.random.sample():生成一个指定范围内的整数随机数，可以生成指定形状的随机数数组。
numpy.random.randint():生成一个指定范围内均匀分布的随机数，可以生成指定形状的随机数数组
numpy.random.uniform():numpy.random.normal():生成一个指定均值和标准差的正态分布的随机数，可以生成指定形状的随机数数组。
numpy.random.gamma():生成一个指定形状和尺度参数的伽马分布的随机数，可以生成指定形状的随机数数组。
numpy.random.binomial():生成一个指定样本量和概率的二项分布的随机数，可以生成指定形状的随机数数组。
random.poisson():生成一个指定均值的泊松分布的随机数，可以生成指定形
状的随机数数组。

6 Ndarry随机数处理

## Slide 21

6.3 随机抽样NumPy提供了许多随机抽样函数，主要包括:
numpy.random.choice():从指定序列中随机抽取元素，可以指定抽取元素的数量和是否允许重复抽取。
numpy.random.shuffle():对指定序列进行随机排序，原序列被修改。
numpy.random.permutation():对指定序列进行随机排序，返回新的随机序列，原序列不变。
numpy.random.sample():从[0,1)之间均匀分布中抽取随机数。
6.4 随机模拟实验利用NumPv提供的随机生成函数，可以进行一些模拟实验本节分别以掷骰子模拟实验与蒙特卡罗模拟实验为例，解释如何使用Numpy模块随机数解决实际随机问题
1.掷骰子模拟实验
假设我们想要模拟掷 100 次骰子，其中每个面出现的概率为 1/6，这个实验可以使用numpy.random.choice(a,size,replace,p)函数完成

6 Ndarry随机数处理

## Slide 22

6.4 随机模拟实验
利用NumPy提供的随机生成函数，可以进行一些模拟实验本节分别以掷骰子模拟实验与蒙特卡罗模拟实验为例，解释如何使用Numpy模块随机数解决实际随机问题。
2.蒙特卡罗模拟实验
蒙特卡罗模拟是一种基于概率统计的方法，通过随机抽样生成伪随机数，模拟某些问题的概率分布及其特性。蒙特卡罗模拟实验可以用于估计概率或期望值、模拟实验或过程、最优化或优化问题求解、统计推断或回归分析等领域。

6 Ndarry随机数处理

## Slide 23

1.Numpy模块的ndarray对象的基本操作有哪些?完成这些操作需要哪些函数和方法?

2.Numpy求解线性方程组的步骤什么?具体如何实现?

3.蒙特卡罗模型的原理是什么?如何利用蒙特卡罗模型预测股票价格?

思考题

